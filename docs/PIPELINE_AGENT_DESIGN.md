# 파이프라인 슈퍼바이저 에이전트 시스템 설계

## 개요

기존 `run_automation_pipeline()` 함수를 슈퍼바이저 에이전트 기반 아키텍처로 전환하여:
- 대본 특성에 따른 **동적 전략 결정**
- **병렬 처리 최적화**로 속도 향상
- **품질 피드백 루프**로 결과물 품질 향상
- **실패 자동 복구** 및 대안 선택

## 에이전트 계층 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                    🎯 VideoSupervisorAgent                          │
│                         (총괄 에이전트)                              │
│                                                                      │
│  역할:                                                               │
│  - 대본 분석 → 영상 특성 파악 (뉴스/드라마/미스터리 등)              │
│  - 최적 전략 결정 (이미지 스타일, 음성, BGM 등)                      │
│  - 하위 에이전트 스케줄링 및 병렬 처리 조율                          │
│  - 품질 검증 결과에 따른 재작업 지시                                 │
│  - 예산 동적 분배                                                    │
│  - 최종 결과 승인                                                    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ 📝 AnalysisAgent│ │ 🎨 CreativeAgent│ │ 🎬 ProductionAgent│
│   (분석 담당)   │ │   (창작 담당)   │ │   (제작 담당)   │
│                 │ │                 │ │                 │
│ - GPT-5.1 분석  │ │ - 씬 이미지     │ │ - FFmpeg 영상   │
│ - 씬 분할      │ │ - 썸네일        │ │ - 자막 합성     │
│ - 메타데이터   │ │ - 스타일 결정   │ │ - 전환 효과     │
│ - 전략 제안    │ │ - 비주얼 톤     │ │ - BGM/SFX       │
└────────┬────────┘ └────────┬────────┘ └────────┬────────┘
         │                   │                   │
         │                   │                   │
         ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ 🎙️ AudioAgent  │ │ 📊 QualityAgent │ │ 📤 PublishAgent │
│   (음성 담당)   │ │   (품질 검증)   │ │   (배포 담당)   │
│                 │ │                 │ │                 │
│ - TTS 생성     │ │ - 이미지 품질   │ │ - YouTube 업로드│
│ - 자막 동기화  │ │ - 오디오 품질   │ │ - 썸네일 설정   │
│ - BGM 선택    │ │ - 영상 검증     │ │ - 플레이리스트  │
│ - SFX 삽입    │ │ - 피드백 생성   │ │ - 쇼츠 생성     │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

## 에이전트별 상세 역할

### 1. VideoSupervisorAgent (총괄)

```python
class VideoSupervisorAgent:
    """
    전체 영상 생성 파이프라인 총괄

    핵심 역할:
    1. 대본 분석 결과를 바탕으로 최적 전략 수립
    2. 하위 에이전트 동적 배치 및 병렬 처리
    3. 품질 검증 결과에 따른 재작업 루프
    4. 예산 동적 분배 (품질 낮으면 재분배)
    """

    async def execute(self, row_data, row_number):
        # Phase 0: 전략 수립
        strategy = await self.plan_strategy(row_data)

        # Phase 1: 분석 (AnalysisAgent)
        analysis = await self.analysis_agent.execute(strategy)

        # Phase 2: TTS 생성 (AudioAgent) - 먼저 실행 (저비용)
        audio = await self.audio_agent.execute(analysis)

        # Phase 3: 병렬 처리
        # - CreativeAgent: 이미지 + 썸네일
        # - 품질 검증 후 필요시 재생성
        creative = await self.creative_agent.execute(analysis, audio)

        # Phase 4: 품질 검증 루프
        quality = await self.quality_agent.execute(analysis, audio, creative)
        if quality.needs_improvement:
            # 피드백 반영하여 재생성
            ...

        # Phase 5: 영상 제작 (ProductionAgent)
        video = await self.production_agent.execute(audio, creative)

        # Phase 6: 배포 (PublishAgent)
        result = await self.publish_agent.execute(video)

        return result
```

### 2. AnalysisAgent (분석)

```python
class AnalysisAgent:
    """
    대본 분석 및 전략 제안

    역할:
    - GPT-5.1로 대본 분석
    - 씬 분할 및 나레이션 추출
    - 카테고리 감지 (news/story/mystery)
    - 최적 전략 제안 (이미지 스타일, 음성 등)
    """

    async def execute(self, strategy):
        # 1. 채널 스타일 분석 (TUBELENS 캐시)
        channel_style = await self.analyze_channel_style(strategy.channel_id)

        # 2. GPT-5.1 대본 분석
        analysis = await self.analyze_script(
            script=strategy.script,
            channel_style=channel_style
        )

        # 3. 전략 제안
        recommendations = self.generate_recommendations(analysis)

        return AnalysisResult(
            scenes=analysis.scenes,
            youtube=analysis.youtube,
            thumbnail=analysis.thumbnail,
            video_effects=analysis.video_effects,
            recommendations=recommendations
        )
```

### 3. AudioAgent (음성)

```python
class AudioAgent:
    """
    TTS + 자막 + BGM + SFX 담당

    역할:
    - TTS 생성 (Google Cloud / Gemini)
    - 자막 동기화 (문장 단위)
    - BGM 선택 (대본 분위기 기반)
    - SFX 삽입 (씬 전환, 강조)
    """

    async def execute(self, analysis):
        # 1. TTS 생성
        tts_result = await self.generate_tts(
            scenes=analysis.scenes,
            voice=analysis.recommendations.voice
        )

        # 2. 자막 동기화
        subtitles = await self.sync_subtitles(tts_result)

        # 3. BGM 선택
        bgm = await self.select_bgm(
            mood=analysis.video_effects.bgm_mood,
            scene_changes=analysis.video_effects.scene_bgm_changes
        )

        # 4. SFX 삽입 포인트
        sfx = await self.plan_sfx(
            sound_effects=analysis.video_effects.sound_effects
        )

        return AudioResult(
            tts=tts_result,
            subtitles=subtitles,
            bgm=bgm,
            sfx=sfx
        )
```

### 4. CreativeAgent (창작)

```python
class CreativeAgent:
    """
    이미지 + 썸네일 생성 담당

    역할:
    - 씬별 이미지 생성 (병렬)
    - 썸네일 생성 (Gemini 3 Pro)
    - 스타일 일관성 유지
    - 캐시 활용 최적화
    """

    async def execute(self, analysis, audio):
        # 1. 이미지 프롬프트 최적화
        optimized_prompts = await self.optimize_prompts(
            scenes=analysis.scenes,
            style=analysis.recommendations.image_style
        )

        # 2. 캐시 확인
        cache_result = await self.check_cache(optimized_prompts)

        # 3. 이미지 생성 (병렬)
        images = await self.generate_images_parallel(
            prompts=optimized_prompts,
            cached=cache_result
        )

        # 4. 썸네일 생성
        thumbnail = await self.generate_thumbnail(
            analysis=analysis,
            title=analysis.youtube.title
        )

        return CreativeResult(
            images=images,
            thumbnail=thumbnail,
            cache_hits=cache_result.hits
        )
```

### 5. QualityAgent (품질)

```python
class QualityAgent:
    """
    품질 검증 및 피드백 생성

    역할:
    - 이미지 품질 검증 (해상도, 스타일 일관성)
    - 오디오 품질 검증 (TTS 자연스러움)
    - 자막 동기화 검증
    - 개선 피드백 생성
    """

    async def execute(self, analysis, audio, creative):
        checks = {
            "images": await self.check_images(creative.images),
            "audio": await self.check_audio(audio.tts),
            "subtitles": await self.check_subtitles(audio.subtitles),
            "consistency": await self.check_style_consistency(creative.images)
        }

        # 전체 품질 점수
        quality_score = self.calculate_score(checks)

        # 개선 필요 항목 판단
        needs_improvement = quality_score < 0.8
        improvement_targets = [
            k for k, v in checks.items() if v.score < 0.7
        ]

        return QualityResult(
            score=quality_score,
            checks=checks,
            needs_improvement=needs_improvement,
            improvement_targets=improvement_targets,
            feedback=self.generate_feedback(checks)
        )
```

### 6. ProductionAgent (제작)

```python
class ProductionAgent:
    """
    FFmpeg 영상 제작 담당

    역할:
    - 클립 생성 (이미지 + 오디오)
    - 자막 burn-in
    - 전환 효과 적용
    - BGM/SFX 믹싱
    - 최종 영상 검증
    """

    async def execute(self, audio, creative):
        # 1. 씬별 클립 생성 (병렬)
        clips = await self.generate_clips_parallel(
            images=creative.images,
            audio=audio.tts
        )

        # 2. 전환 효과 적용
        clips_with_transitions = await self.apply_transitions(
            clips=clips,
            style=audio.transitions
        )

        # 3. 오디오 믹싱 (BGM + SFX)
        mixed_audio = await self.mix_audio(
            tts=audio.tts,
            bgm=audio.bgm,
            sfx=audio.sfx
        )

        # 4. 최종 합성
        video = await self.concat_and_render(
            clips=clips_with_transitions,
            audio=mixed_audio,
            subtitles=audio.subtitles
        )

        # 5. 영상 검증
        validation = await self.validate_video(video)

        return ProductionResult(
            video=video,
            duration=validation.duration,
            size=validation.size,
            is_valid=validation.is_valid
        )
```

### 7. PublishAgent (배포)

```python
class PublishAgent:
    """
    YouTube 업로드 담당

    역할:
    - 영상 업로드
    - 썸네일 설정
    - 메타데이터 설정 (제목, 설명, 태그)
    - 플레이리스트 추가
    - 예약 공개 설정
    - 쇼츠 생성 및 업로드
    """

    async def execute(self, video, analysis, creative):
        # 1. 메인 영상 업로드
        upload_result = await self.upload_video(
            video=video.video,
            title=analysis.youtube.title,
            description=analysis.youtube.description,
            tags=analysis.youtube.tags,
            privacy=self.config.privacy,
            publish_at=self.config.publish_at
        )

        # 2. 썸네일 설정
        await self.set_thumbnail(
            video_id=upload_result.video_id,
            thumbnail=creative.thumbnail
        )

        # 3. 플레이리스트 추가
        if self.config.playlist_id:
            await self.add_to_playlist(
                video_id=upload_result.video_id,
                playlist_id=self.config.playlist_id
            )

        # 4. 쇼츠 생성 (옵션)
        shorts_url = None
        if analysis.video_effects.shorts:
            shorts_url = await self.generate_and_upload_shorts(
                video=video,
                shorts_config=analysis.video_effects.shorts
            )

        return PublishResult(
            video_url=upload_result.url,
            shorts_url=shorts_url,
            video_id=upload_result.video_id
        )
```

## 파이프라인 실행 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. Google Sheets에서 '대기' 행 조회                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 2. VideoSupervisorAgent.plan_strategy()                              │
│    - 대본 길이 분석 → 이미지 개수 결정                               │
│    - 카테고리 추정 → 스타일 결정                                     │
│    - 채널 분석 → 톤앤매너 결정                                       │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 3. AnalysisAgent.execute()                                           │
│    - GPT-5.1 대본 분석                                               │
│    - 씬 분할 + 메타데이터 생성                                        │
│    - video_effects 생성 (BGM, SFX, 전환)                             │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 4. AudioAgent.execute()                                              │
│    - TTS 생성 (먼저 실행 - 저비용)                                   │
│    - 자막 동기화                                                     │
│    - BGM 선택                                                        │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 5. 병렬 처리                                                         │
│    ┌─────────────────────────────────────────────────────────────┐  │
│    │ CreativeAgent.execute()                                      │  │
│    │ - 씬 이미지 생성 (병렬)                                       │  │
│    │ - 썸네일 생성                                                 │  │
│    └─────────────────────────────────────────────────────────────┘  │
│    ┌─────────────────────────────────────────────────────────────┐  │
│    │ QualityAgent.pre_check()                                     │  │
│    │ - TTS 품질 검증                                               │  │
│    │ - 자막 동기화 검증                                            │  │
│    └─────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 6. QualityAgent.full_check() - 품질 검증 루프                        │
│                                                                      │
│    ┌─────────────────────────────────────────────────────────────┐  │
│    │ 품질 점수 < 0.8?                                             │  │
│    │   ├─ YES → 피드백 생성 → 해당 에이전트 재실행               │  │
│    │   │        (최대 3회 시도)                                   │  │
│    │   └─ NO  → 다음 단계 진행                                   │  │
│    └─────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 7. ProductionAgent.execute()                                         │
│    - 클립 생성 (병렬)                                                │
│    - 전환 효과 적용                                                  │
│    - 오디오 믹싱                                                     │
│    - 최종 렌더링                                                     │
│    - 영상 검증                                                       │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 8. PublishAgent.execute()                                            │
│    - YouTube 업로드                                                  │
│    - 썸네일 설정                                                     │
│    - 플레이리스트 추가                                               │
│    - 쇼츠 생성 (옵션)                                                │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 9. Google Sheets 업데이트                                            │
│    - 상태 = '완료'                                                   │
│    - 영상URL, 쇼츠URL, 비용 저장                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 예산 관리 시스템

```python
class BudgetManager:
    """
    동적 예산 분배

    기본 분배:
    - 분석 (GPT-5.1): 10%
    - 이미지 생성: 50%
    - TTS: 15%
    - 썸네일: 10%
    - 검수: 5%
    - 여유: 10%

    동적 재분배:
    - 품질 점수 낮은 항목에 예산 추가
    - 캐시 히트로 절감된 예산 재분배
    """

    def reallocate(self, from_agent, to_agent, amount):
        """예산 재분배"""
        if self.get_remaining(from_agent) >= amount:
            self.allocation[from_agent] -= amount
            self.allocation[to_agent] += amount
            return True
        return False
```

## 품질 피드백 루프

```
┌─────────────────────────────────────────────────────────────────────┐
│                         QualityAgent                                 │
└────────────────────────────┬────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ 이미지 검증   │    │ 오디오 검증   │    │ 일관성 검증   │
│              │    │              │    │              │
│ - 해상도     │    │ - 음질       │    │ - 스타일 통일 │
│ - 노이즈     │    │ - 발음       │    │ - 톤앤매너   │
│ - 프롬프트   │    │ - 속도       │    │ - 연속성     │
│   일치도     │    │ - 자막동기화 │    │              │
└───────┬───────┘    └───────┬───────┘    └───────┬───────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                             ▼
                    ┌───────────────┐
                    │ 품질 점수     │
                    │ 계산          │
                    │              │
                    │ score < 0.8  │
                    │ → 재생성     │
                    │              │
                    │ score >= 0.8 │
                    │ → 통과       │
                    └───────────────┘
```

## 파일 구조

```
scripts/video_pipeline/agents/
├── __init__.py
├── base.py                    # BaseAgent, AgentResult, TaskContext
├── supervisor.py              # VideoSupervisorAgent (총괄)
├── analysis_agent.py          # AnalysisAgent (대본 분석)
├── audio_agent.py             # AudioAgent (TTS + BGM + SFX)
├── creative_agent.py          # CreativeAgent (이미지 + 썸네일)
├── quality_agent.py           # QualityAgent (품질 검증)
├── production_agent.py        # ProductionAgent (영상 제작)
├── publish_agent.py           # PublishAgent (YouTube 업로드)
├── budget_manager.py          # BudgetManager (예산 관리)
├── strategy.py                # 전략 수립 로직
└── utils.py                   # 유틸리티 함수
```

## 기대 효과

### 1. 성능 향상
- **병렬 처리**: 이미지 생성 + 품질 검증 동시 실행 → ~1.5배 속도 향상
- **캐시 최적화**: 유사 프롬프트 이미지 재사용 → 비용 20% 절감

### 2. 품질 향상
- **피드백 루프**: 품질 미달 시 자동 재생성 (최대 3회)
- **일관성 검증**: 이미지 스타일 통일성 검사

### 3. 유연성
- **동적 전략**: 대본 특성에 따른 자동 설정 조정
- **예산 재분배**: 품질 이슈 발생 시 예산 동적 할당

### 4. 유지보수성
- **모듈화**: 각 에이전트 독립적으로 테스트/수정 가능
- **확장성**: 새 기능 추가 시 새 에이전트만 추가

## 구현 우선순위

1. **Phase 1**: 기본 구조 (base.py, supervisor.py)
2. **Phase 2**: 핵심 에이전트 (analysis, audio, creative)
3. **Phase 3**: 품질 검증 (quality_agent.py)
4. **Phase 4**: 제작/배포 (production, publish)
5. **Phase 5**: 예산 관리 + 피드백 루프
6. **Phase 6**: 기존 파이프라인 통합

## 다음 단계

1. 공통 에이전트 코드(`scripts/common/`) 활용
2. `scripts/video_pipeline/agents/` 폴더 활용
3. 각 에이전트 순차 구현
4. `run_automation_pipeline()` 함수 리팩토링
