# PC 서버 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 키 값을 입력하세요
# cp .env.example .env

# Google Cloud API Key (TTS용)
export GOOGLE_CLOUD_API_KEY="your-google-cloud-api-key"

# Google API Key (이미지 생성용 - AI Studio에서 발급)
export GOOGLE_API_KEY="your-google-api-key"

# OpenRouter API Key (대본 생성용 - Claude Opus)
export OPENROUTER_API_KEY="your-openrouter-api-key"
